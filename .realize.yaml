settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: server
  path: ./server
  commands: {}
  watcher:
    extensions:
    - go
    paths:
    - /
    ignored_paths:
    - .git
    - .realize
    - vendor
